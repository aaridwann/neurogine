import { StyleSheet } from "react-native";

export default StyleSheet.create({
    LoadingFallback: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    Container: {
        flex: 1
    }
});